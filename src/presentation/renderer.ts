// Renderer process entry point for TypeScript compilation
// This file ensures TypeScript finds something to compile in the renderer process

export {};

// Re-export the renderer logic (will be bundled separately)
console.log('Renderer process initialized');